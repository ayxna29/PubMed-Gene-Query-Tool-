# PubMed-Gene-Query-Tool-
A Shiny app for systematic literature mining across gene lists. Users upload a gene list and define keyword search sets; the app queries PubMed via the NCBI Entrez API for each gene–keyword pair and returns the top N studies in an interactive, filterable table with titles linked to PubMed
